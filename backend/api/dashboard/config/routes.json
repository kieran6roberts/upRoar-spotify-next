{
  "routes": [
    {
      "method": "GET",
      "path": "/dashboard",
      "handler": "dashboard.find",
      "config": {
        "policies": ["plugins::users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "PUT",
      "path": "/dashboard",
      "handler": "dashboard.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/dashboard",
      "handler": "dashboard.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
